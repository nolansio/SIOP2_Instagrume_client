<nav class="navbar navbar-expand-lg navbar-light navbar-modern sticky-top">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
            <i class="bi bi-flower1 me-2" style="font-size: 1.8rem;"></i>
            <span>Instagrume</span>
        </a>
        
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">
                        <i class="bi bi-house-door me-1"></i> Accueil
                    </a>
                </li>
            </ul>
            
            {# Barre de recherche moderne #}
            <form class="d-flex me-3" action="{{ path('app_user_search_profile') }}" method="get">
                <div class="input-group">
                    <input class="form-control search-modern border-end-0" 
                           type="search" 
                           name="q" 
                           placeholder="Rechercher..." 
                           aria-label="Search">
                    <button class="btn btn-outline-secondary border-start-0 search-modern border-start" 
                            type="submit" 
                            style="border-left: 0;">
                        <i class="bi bi-search"></i>
                    </button>
                </div>
            </form>
            
            {# Navigation pour utilisateur non connecté #}
            {% if app.user is null %}
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_security_login') }}">
                            <i class="bi bi-box-arrow-in-right me-1"></i> Connexion
                        </a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="btn btn-modern btn-primary-modern" style="color: white;" href="{{ path('app_security_register') }}">
                            <i class="bi bi-person-plus me-1"></i> Inscription
                        </a>
                    </li>
                </ul>
            {% else %}
                {# Navigation pour utilisateur connecté #}
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="btn btn-modern btn-primary-modern me-2" href="{{ path('app_publication_new') }}">
                            <i class="bi bi-plus-circle me-1"></i> Créer
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" 
                           href="#" 
                           id="navbarDropdown" 
                           role="button" 
                           data-bs-toggle="dropdown">
                            <img src="https://ui-avatars.com/api/?name={{ app.user.username ?? 'User' }}&background=10b981&color=fff" 
                                 class="rounded-circle avatar-modern me-2" 
                                 width="35" 
                                 height="35"
                                 alt="Avatar">
                            <span class="fw-semibold">{{ app.user.username ?? 'Profil' }}</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0" style="border-radius: 12px;">
                            <li>
                                <a class="dropdown-item py-2" href="{{ path('app_user_profile') }}">
                                    <i class="bi bi-person me-2"></i> Mon profil
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item py-2" href="{{ path('app_user_edit_profile') }}">
                                    <i class="bi bi-gear me-2"></i> Paramètres
                                </a>
                            </li>
                            {# Si modérateur - à décommenter quand les rôles seront gérés #}
                            {# {% if 'ROLE_MODERATOR' in app.user.roles %} #}
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item py-2" href="{{ path('app_moderation') }}">
                                    <i class="bi bi-shield-check me-2 text-warning"></i> Modération
                                </a>
                            </li>
                            {# {% endif %} #}
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item py-2 text-danger" href="#">
                                    <i class="bi bi-box-arrow-right me-2"></i> Déconnexion
                                </a>
                            </li>
                        </ul>
                    </li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>
